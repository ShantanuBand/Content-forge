{
  "name": "content-pilot",
  "version": "1.0.0",
  "description": "ContentForge Fullstack Deployment",
  "scripts": {
    "install": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start"
  }
}
